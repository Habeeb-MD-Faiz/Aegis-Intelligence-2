# Direct dependencies only. Everything else pip resolves.
#
# This was a full `pip freeze` — 57 lines, most of them transitives. That kind
# of file cannot be maintained: bumping fastapi means hand-re-resolving
# starlette, anyio, h11 and sniffio, so nobody does it and the pins rot in
# place. It also hid *why* things were installed — bitarray, ckzg, rlp and
# parsimonious are the eth stack; a dozen more came in with langchain — and
# nothing in the file separated "we chose this" from "something dragged it in".
#
# Direct deps stay pinned. Transitives are pip's problem, and CI installs from
# this file on every push, so a bad resolution surfaces immediately.

# --- API -------------------------------------------------------------------
fastapi==0.141.1
uvicorn==0.52.0            # ASGI server; invoked from the command line
pydantic==2.13.4

# --- HTTP ------------------------------------------------------------------
requests==2.34.2           # guard._execute_outbound and anchor's JSON-RPC calls

# --- Configuration ---------------------------------------------------------
python-dotenv==1.2.2       # load_dotenv() in policy_builder

# --- On-chain anchoring (optional at runtime) ------------------------------
eth-account==0.13.7        # transaction construction, signing, recovery

# --- LLM policy suggestions (optional at runtime; never in the guard path) --
langchain-core==1.5.4
langchain-groq==1.1.3

# --- Tests -----------------------------------------------------------------
pytest==9.1.1              # CI previously installed this ad-hoc
